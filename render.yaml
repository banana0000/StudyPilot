services:
  - type: web
    name: studypilot
    runtime: python
    buildCommand: pip install -r requirements.txt
    plan: free
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 app:server
    healthCheckPath: /
    autoDeployTrigger: commit
